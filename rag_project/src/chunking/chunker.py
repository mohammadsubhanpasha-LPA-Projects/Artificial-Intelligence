"""
Document Chunker.
"""
from typing import List, Dict

class TextChunker:
    """Splits text into manageable chunks."""
    def chunk(self, documents: List[Dict[str, str]]) -> List[Dict[str, str]]:
        """Chunk the provided documents."""
        return documents
